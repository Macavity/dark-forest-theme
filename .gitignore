node_modules/
.DS_Store
*.log
.pnpm-store/

# Build output — populated by `bun run build` (fonts + theme files).
dist/

# Release artifacts — built by `bun run build:zip`, uploaded to GitHub releases.
dark-forest-*.zip
