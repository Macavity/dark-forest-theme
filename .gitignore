node_modules/
.DS_Store
*.log
.pnpm-store/

# Fonts — fetched from @fontsource on first build, treated as a build artifact.
fonts/

# Build output — populated by `bun run build` (fonts + theme files).
dist/

# Release artifact — built by `bun run build:zip`, uploaded to GitHub releases.
package.zip
