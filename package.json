{
  "name": "@dark-forest-theme",
  "version": "1.0.1",
  "private": true,
  "description": "A moss-green dark forest theme.",
  "license": "MIT",
  "author": "Alexander Pape",
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "build:fonts": "bun run scripts/build-fonts.ts",
    "build:zip": "bun run scripts/build-zip.ts",
    "validate": "bun run scripts/validate-manifest.ts",
    "install:theme": "bun run scripts/install.ts",
    "version:bump": "bun run scripts/update-version.ts"
  },
  "devDependencies": {
    "@fontsource-variable/dm-sans": "^5.2.0",
    "@fontsource/cormorant-garamond": "^5.2.0",
    "@fontsource/jetbrains-mono": "^5.2.0",
    "@grove-notes/manifest-schema": "^0.1.1",
    "@types/bun": "^1.3.0"
  }
}
